# Optional environment overrides for the API (set in your shell or IDE).
# ASP.NET Core does not load this file automatically — copy values as needed.

# HTTP listen port (overrides Server:Port from appsettings when set)
PORT=8080

# Postgres (alternative to ConnectionStrings:DefaultConnection in appsettings)
# DATABASE_URL=postgres://darts:darts123@localhost:5432/dartsdb
